# v0.1 Readiness Gap List

This checklist tracks release-blocking gaps before implementation starts.

## Scope Guard

```txt
[ ] no file editing tool exists
[ ] no patch application path exists
[ ] no persistent memory exists
[ ] no product-level subagent orchestration exists
[ ] no MCP integration exists
[ ] no sandbox runtime is claimed
[ ] no eval dashboard is introduced
[ ] no IDE extension is introduced
[ ] no background task mode is introduced
```

## Contract Blockers

```txt
[ ] F-06 strict tool validation rejects invalid and extra args
[ ] F-11 restricted commands pause for approval
[ ] S-02 symlink escape is denied
[ ] S-05 shell strings are denied
[ ] S-06 destructive commands are denied
[ ] S-07 truncation metadata is explicit
[ ] S-09 traces redact secrets and API keys
```

## Required Specs Before Coding

```txt
[x] permission gate and strict tool validation spec exists
[x] output bounds and truncation spec exists
[x] provider normalization spec exists if provider protocol changes
[x] TUI interaction spec exists if prompt behavior changes
```

## Required Evidence Before Release

```txt
[ ] bun run format:check passes
[ ] bun run lint passes
[ ] bun run typecheck passes
[ ] bun run test passes
[ ] bun run smoke passes
[ ] live smoke documented as skipped or passed
[ ] release note written
[ ] known limitations documented
```
